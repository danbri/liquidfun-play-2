// varying vec4 pos;
// uniform vec2 middle;
// uniform bool is_circle;

void main(void) {
  // if (is_circle && distance(pos.xy, middle) > r) {
  //   discard;
  // }
  gl_FragColor = vec4(0.2, 0.7, 0.1, 0.9);
}
